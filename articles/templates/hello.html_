{% extends "base.html" %}
{% block content %}
    <h1>Hello, {{ name }}</h1>
    <p>태그 {{ tags }}</p>
    <ul>
        {% for book in books %}
            <li>{{ book }}</li>
        {% endfor %}
    </ul>
{% endblock content %}

